name: Hello World Workflow

# Triggers the workflow manually using the "workflow_dispatch" event
on:
  push: 
    branches:
      - testbranch

  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
    - name: Print Hello World
      run: echo "Hello, World!"
