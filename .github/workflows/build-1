name: Build

on:
  push:
    branches:
      - 'testbranch'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        run: |
          echo "This workflow is running fine..."
